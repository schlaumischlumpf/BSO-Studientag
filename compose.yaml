services:
  proxy:
    image: nginx:alpine
    container_name: berufsmesse_proxy
    depends_on:
      - app
    ports:
      - "9000:80"
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      # falls deine App statische Dateien direkt über nginx ausliefern soll:
      - .:/var/www/html:ro
    networks:
      - berufsmesse_net

  app:
    build:
      context: .
    container_name: berufsmesse_app
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_HOST: db
      DB_USER: root
      DB_PASS: "test"
      DB_NAME: berufsmesse
    volumes:
      - .:/var/www/html
    networks:
      - berufsmesse_net

  db:
    image: mariadb:10.9
    container_name: berufsmesse_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: "test"
      MYSQL_DATABASE: berufsmesse
    volumes:
      - db-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - berufsmesse_net

  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    container_name: berufsmesse_phpmyadmin
    restart: always
    depends_on:
      db:
        condition: service_healthy
    environment:
      PMA_HOST: db
      PMA_USER: root
      PMA_PASSWORD: "test"
      PHP_UPLOAD_MAX_FILESIZE: 16M
    ports:
      - "8082:80"
    networks:
      - berufsmesse_net

volumes:
  db-data:

networks:
  berufsmesse_net:
    driver: bridge
